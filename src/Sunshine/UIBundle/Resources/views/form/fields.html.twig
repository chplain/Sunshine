{% block form_row -%}
    {#<div class="form-group{% if (not compound or force_error|default(false)) and not valid %} has-error{% endif %} form-inline">
        {{- form_label(form) -}}
        {{- form_widget(form) -}}
        {{- form_errors(form) -}}
    </div>#}
    <div class="form-group{% if (not compound or force_error|default(false)) and not valid %} has-error{% endif %} form-inline">
        {{- form_label(form,'',{ 'label_attr': {'class': 'col-sm-2'} }) -}}
        <div class="col-sm-10">
            {{- form_widget(form) -}}
        </div>
        {{- form_errors(form) -}}
    </div>
{%- endblock form_row %}